{
  "name": "streamlit-aggrid-redux",
  "packageManager": "yarn@3.2.1",
  "version": "0.3.3",
  "repository": "https://github.com/jwood983/streamlit-aggrid-redux.git",
  "license": "MIT",
  "author": "JD Wood",
  "private": true,
  "workspaces": [
    "streamlit_aggrid_redux/**/frontend*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -v -i run build && poetry build",
    "develop": "yarn workspaces foreach -v -j 99 -i -p run start"
  },
  "resolutions": {
    "streamlit-component-lib/apache-arrow": "9.0.0"
  }
}
